import { Header } from '../Header/header';
import { CartList } from './CartList';
export const Cart = () => {
    return (
        <>
            <Header />
            <CartList />
        </>
    );
};
